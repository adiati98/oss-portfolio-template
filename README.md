# Curated Open Source Portfolio Template

> [!TIP]
> You can see examples of the finished portfolio generated by this tool here:
>
> - **Static website (HTML):** https://adiati98.github.io/oss-portfolio-template/
> - **Markdown:** https://github.com/adiati98/oss-portfolio-template/blob/demo/contributions/markdown-generated/README.md

This repository is a **template** that automatically generates a curated portfolio of your Open Source Software (OSS) contributions. It tracks Pull Requests (PRs), issues, code reviews, co-authored commits, and collaborations, providing a detailed, organized record of your impact in the community.

The content updates automatically via a Node.js script and a GitHub Actions workflow.

---

## 🛠️ Quick Start Guide

### 1. Repository Setup

Choose how you want to start your project:

- **Option A:** Click **Use this template** at the top of this repository.
- **Option B:** Click **Fork** to create a linked copy, then clone it to your local machine:

  ```bash
  git clone https://github.com/adiati98/oss-portfolio-template.git
  ```

### 2. Environment Setup

Ensure [Node.js](https://nodejs.org/en) is installed and run:

```bash
npm ci
```

### 3. Update Configuration

Open `scripts/config/config.js` and and update your GitHub handle.

```javascript
// scripts/config/config.js

// Change this to your GitHub handle
const GITHUB_USERNAME = 'your-github-username';
```

### 4. Running Locally

To generate your files locally, you need a Personal Access Token (PAT) with `public_repo` scope.

1. **Create `.env` file**

   Copy the example environment file:

   ```bash
   cp .env.example .env
   ```

2. **Add your token**

   Open `.env` and replace `your_token_here` with your GitHub PAT.

3. **Initialize and run the project**

   Before your very first run, clear any template example data, then start the script:

   ```bash
   npm run clean
   npm start
   ```

### 5. Set up GitHub Actions (Automated Runs)

GitHub Actions remain disabled by default in repositories generated from a template.

1. Navigate to the **Actions** tab in your repository and click "**I understand my workflows, go ahead and enable them.**"

2. Select the **Update Contributions** workflow and click **Run workflow** to execute the first sync manually. The workflow uses the built-in `GITHUB_TOKEN`, so there is nothing else to configure.

3. **Turn on automatic updates.** Once your first manual run looks right, open `.github/workflows/update-contributions.yml` and uncomment the `schedule:` block near the top. From then on it updates daily, with a full re-sync on the 1st of each month.

> [!NOTE]
> Nothing runs on its own until you uncomment the schedule.
>
> Run it manually first. A typo in your handle shows up in a minute instead of a day.
>
> Scheduled runs can start late or be skipped when GitHub is busy. That's normal.

---

## 📂 Accessing Your Reports

After the script runs successfully, you can find your generated portfolio in the following locations:

- **GitHub Markdown Portfolio:** Navigate to `contributions/markdown-generated/README.md`. This is the main "front page" of your logs on GitHub. The detailed breakdowns for every three months are stored in `contributions/markdown-generated/YYYY/`.
- **Static Website Files:** If you are hosting a website, the ready-to-use HTML files are in `contributions/html-generated/`.

---

## ⭐ Featuring Your Best Work (Optional)

Want to feature specific work with your own story behind it? Add entries to
`data/highlights.json` like this:

```json
// data/highlights.json
[
  {
    "type": "project",
    "title": "OSS Portfolio",
    "org": "Personal · 2025–present",
    "url": "https://github.com/your-github-username/oss-portfolio",
    "description": "Why this one made the list."
  }
]
```

### Highlight Types

The `type` field picks the label shown on each card. Choose from the list below, or make up your
own — nothing is enforced.

| Type            | Icon | Type            | Icon | Type          | Icon |
| :-------------- | :--- | :-------------- | :--- | :------------ | :--- |
| `award`         | 🏆   | `project`       | 🛠   | `docs`        | 📚   |
| `talk`          | 🎤   | `keynote`       | 🎙️   | `panel`       | 🎙️   |
| `video`         | 🎥   | `article`       | 📝   | `publication` | 📄   |
| `course`        | 🎓   | `tutorial`      | 📖   | `guide`       | 🗺️   |
| `pr`            | 🔀   | `review`        | 👀   | `feature`     | ✨   |
| `library`       | 📦   | `tool`          | ⚙️   | `framework`   | 🏗️   |
| `workshop`      | 🔧   | `research`      | 🔬   | `mentorship`  | 🤝   |
| `leadership`    | 👨‍💼   | `maintainer`    | 🛡️   | `community`   | 🌍   |
| `conference`    | 🎪   | `recognition`   | 🌟   | `achievement` | 🎯   |
| `collaboration` | 🤲   | `vulnerability` | 🔐   | —             | —    |

**Not in the list?** Add your own type in `data/highlights.json`:

```json
{
  "type": "open-source-win",
  "icon": "🚀",
  "title": "OSS Portfolio"
}
```

Now the card shows **🚀 Open Source Win** instead. And if you skip `type` altogether, the card
just shows **⭐ Highlight**.

---

Leave the file out entirely, or leave it as an empty array, and the **Highlights** page and its
nav link still generate — same as Quarterly Reports and Glossary — just with a card showing
"No highlights at the moment." Only the landing-page teaser card is conditional: no data means
no card, so the landing page never promotes an empty page.

---

## 🌐 Publishing the Static Website (Optional)

The generated HTML lives in `contributions/html-generated/` and is committed to your repository, so **any static host can publish it.**

### Netlify, Vercel, Cloudflare Pages — no setup needed

Connect the repo and set the publish directory to:

```
contributions/html-generated
```

### GitHub Pages — one workflow, included

1. **Settings → Pages → Source → GitHub Actions**.
2. Enable `.github/workflows/deploy-pages.yml` on the Actions tab.

It republishes after every **Update Contributions** run, or on demand with **Run workflow**.

> [!TIP]
> Not using GitHub Pages? Delete `.github/workflows/deploy-pages.yml`. Nothing else depends on it.

### 🎨 Customizing Your Brand Colors

To use your own brand colors, update the five values in `scripts/config/theme.js`:

```javascript
// scripts/config/theme.js

module.exports = {
  brand: '#4338CA', // your main brand color
  positive: '#10b981', // color for open/positive items
  caution: '#A16207', // color for items that need attention
  critical: '#ef4444', // color for closed/blocked items
  neutral: '#6b7280', // color for muted text and borders
};
```

The rest of the site — light mode, dark mode, borders, badges, and the favicon — updates automatically to match.

Want more control? The same file also has optional overrides, commented out by default:

- `accent` — a second color for playful highlights, like the persona badge
- `surface` — the background color for light mode
- `ink` — the main text color for light mode

---

## 💡 How It Works

An automation pipeline fetches, processes, and presents your GitHub activity using a Node.js script and a GitHub Actions workflow.

### The Brain: The Automation Script

The script follows a clear pipeline: fetch data, process it, and generate output files.

#### 1. Data Fetching & Processing

- **GitHub API (v3):** The script communicates with the GitHub REST API to collect five distinct types of community activity: **Merged PRs, Issues, Reviewed PRs, Co-authored PRs, and Collaborations**.
- **Auto-Discovery Logic:** The system automatically scans your entire contribution history to determine your "Active Since" date.
- **Smart Syncing:** The script performs either a fast incremental update (fetching only recent activity) or a full sync to ensure it captures every historical contribution.
- **Deduplication:** The script resolves the latest status for every contribution and deduplicates items that might appear in multiple search queries.
- **Caching:** The script maintains `pr-cache.json` and `commit-cache.json` to dramatically speed up future runs by skipping already processed pull request data and co-author commit history.

#### 2. Output Generation (Markdown & HTML)

- **Markdown Reports:** The script generates detailed quarterly logs (e.g., `2024/Q1-2024.md`) and a summary `README.md` in the `contributions/markdown-generated` folder. These include visual progress bars and a list of your primary focus projects.
- **HTML Reports (Optional):** The script generates a Tailwind-styled landing page and interactive dashboards in the `contributions/html-generated` folder. These serve as a professional static website.

#### 3. Collaboration Profiles

The "All-Time Impact" dashboard automatically assigns a persona title based on your contribution data patterns. This helps viewers quickly understand your primary impact style within the open source ecosystem.

| Priority | Persona Title             | Focus                                          |
| :------- | :------------------------ | :--------------------------------------------- |
| 1        | **Community Mentor**      | Code review and technical guidance.            |
| 2        | **Core Contributor**      | Feature development and bug fixing.            |
| 3        | **Project Architect**     | Problem identification and feature planning.   |
| 4        | **Collaborative Partner** | Pair programming and co-authoring code.        |
| 5        | **Ecosystem Partner**     | Technical discussion and community engagement. |

### The Automation: GitHub Action Workflows

The `.github/workflows` folder holds two workflows:

**`update-contributions.yml`** — fetches your activity and commits the regenerated reports back to your branch, using the built-in `GITHUB_TOKEN`. Two modes, both available from the Actions tab:

- **Daily Update:** A lightweight incremental run that captures recent activity.
- **Monthly Sync:** A full sync that clears the caches and re-verifies your entire history.

**`deploy-pages.yml`** — optional, for GitHub Pages users. See [Publishing the Static Website](#-publishing-the-static-website-optional).

> [!NOTE]
> `demo-site.yml` builds this template's own demo. `npm run clean` (Quick Start step 4) removes it from your copy.
